import java.util.*;
public class StringCharat {
public static void main(String args[]) {
	 String str = "this is a demo program it helps to get concept"; 
     String str1=" ";
	
     for (int i=0; i<=str.length()-1; i++) {  
         if(str.charAt(i) == ' ' && str.charAt(i+1)=='i') { 
        	
        	 
         } 
     }  
   
}
}
